"""Scenario synthesis from log + process tree + discovered UCM.

The :mod:`synthesis` module turns a :class:`pm4py_ucm.algo.discovery.\
variants.clustering.ClusteringResult` into a populated
:class:`pm4py_ucm.objects.ucm.obj.UCM.ScenarioGroup` on the UCM, ready
to be exported to ``.jucm`` and traversed inside jUCMNav."""
