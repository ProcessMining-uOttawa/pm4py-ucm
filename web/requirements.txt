# Upper bound: st.components.v1.html (Dashboards island, SVG viewer,
# open-image-in-tab) is deprecated for st.iframe. Cap the major so a
# Streamlit Cloud rebuild can't pull a release that removes it out from
# under the deployed app before the migration lands (see the tracking
# issue). Raise the cap once _embed_html moves to st.iframe.
streamlit>=1.32,<2
pm4py>=2.7
scikit-learn>=1.3
-e .
